import React, { FC } from "react";
import { Text, View } from "react-native";

export const CheckListScreen: FC = () => {
    return <View>
        <Text>
            Checklist screen!
        </Text>
    </View>
}